{
  "name": "ats-thin-pact",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "pact/consumers/ats-thin/src",
  "projectType": "library",
  "tags": ["pact-consumer"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["pact/consumers/ats-thin/**/*.ts"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Pact contract tests are excluded from the aggregate test target. Run nx pact-test ats-thin-pact (or invoke vitest directly) to execute them.'"
      }
    },
    "pact-test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --root pact/consumers/ats-thin"
      }
    }
  }
}
