{
  "name": "canonicalization",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/canonicalization/src",
  "projectType": "library",
  "tags": ["scope:ats"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/canonicalization",
        "main": "libs/canonicalization/src/index.ts",
        "tsConfig": "libs/canonicalization/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/canonicalization/**/*.ts"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --root libs/canonicalization"
      }
    }
  }
}
