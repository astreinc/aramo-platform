{
  "name": "esign",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/esign/src",
  "projectType": "library",
  "tags": ["scope:boundary"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/esign",
        "main": "libs/esign/src/index.ts",
        "tsConfig": "libs/esign/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/esign/**/*.ts"]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --root libs/esign"
      }
    }
  }
}
