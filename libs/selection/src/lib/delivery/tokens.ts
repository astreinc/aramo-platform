// M5 PR-6 §4.3 — DI token for the DeliveryProvider port.
// Injected via @Inject(DELIVERY_PROVIDER_TOKEN) at EngagementController.
// Mirrors the libs/ai-draft DRAFT_PROVIDER_TOKEN pattern (M5 PR-5 §4.5).

export const DELIVERY_PROVIDER_TOKEN = 'DELIVERY_PROVIDER_TOKEN';
