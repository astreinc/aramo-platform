---
import BaseLayout from '../layouts/BaseLayout.astro';
import ContactForm from '../components/ContactForm.tsx';
---

<BaseLayout
  title="Contact — Aramo"
  description="Talk to the team building Aramo. Send a message, or write to hello@aramo.ai."
>
  <!-- Hero + form -->
  <section class="section hero">
    <div class="container">
      <div class="split">
        <div class="split__body">
          <span class="eyebrow">Contact</span>
          <h1 class="hero__display">Talk to us.</h1>
          <p class="hero__sub">
            The people building Aramo run a staffing desk too. Send a message and
            we’ll get back to you — no phone tree, no queue.
          </p>
          <p class="secondary-line">
            Prefer email? Write to&#32;
            <a href="mailto:hello@aramo.ai">hello@aramo.ai</a>.
          </p>
        </div>
        <div class="split__visual">
          <ContactForm client:visible />
        </div>
      </div>
    </div>
  </section>

  <!-- Direct lines (alternatives) -->
  <section class="section section--bleed section--alt">
    <div class="container">
      <ul class="contact-grid">
        <li class="contact-card">
          <p class="contact-card__k">General</p>
          <p class="contact-card__v">
            <a href="mailto:hello@aramo.ai">hello@aramo.ai</a>
          </p>
          <p class="contact-card__note">
            Questions about the product, security, or a partnership.
          </p>
        </li>
        <li class="contact-card">
          <p class="contact-card__k">Get started</p>
          <p class="contact-card__v">
            <a href="/get-started">Request a workspace</a>
          </p>
          <p class="contact-card__note">
            Ready to run your desk on Aramo? Start here.
          </p>
        </li>
      </ul>
    </div>
  </section>

  <!-- CTA -->
  <section class="section section--bleed section--deep cta-band">
    <div class="container">
      <h2 class="section-title">Put proof behind your next submittal.</h2>
      <div class="cluster">
        <a class="btn btn--primary" href="/get-started">Get started</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .secondary-line {
    font-size: var(--fs-14);
    color: var(--c-text-subtle);
    margin: var(--sp-4) 0 0;
  }
  .contact-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--sp-5);
    max-width: 52rem;
  }
  .contact-card {
    background: var(--c-bg);
    border: 1px solid var(--c-border);
    border-radius: var(--r-lg);
    padding: var(--sp-6);
    box-shadow: var(--sh-1);
  }
  .contact-card__k {
    margin: 0;
    font-size: var(--fs-14);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--c-text-subtle);
  }
  .contact-card__v {
    margin: var(--sp-2) 0 0;
    font-size: var(--fs-20);
    font-weight: var(--fw-semibold);
  }
  .contact-card__note {
    margin: var(--sp-3) 0 0;
    color: var(--c-text-muted);
  }
  @media (max-width: 640px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
